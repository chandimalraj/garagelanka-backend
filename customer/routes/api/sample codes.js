    /*    await reqBody.forEach(function(item_list) {
                const item_code = service_types.item_code;
                const desc = service_types.desc;
                const unitPrice = service_types.unitPrice;
                const qnt = service_types.qnt;
                const total = service_types.total;
                
                // Build model Object
                const modelFields = {};
                modelFields.item_code = item_code;
                modelFields.desc = desc;
                modelFields.unitPrice = unitPrice;
                modelFields.qnt = qnt;
                modelFields.total = total;
    
                //Add new model
                item_list  = new item_list(modelFields);
               
            }); */

                  
            /*  await reqBody.forEach(function(service_types) {
                const service_type = service_types.service_type;
                const service_cost = service_types.service_cost;
                const number_of_hrs = service_types.number_of_hrs;
                const total = service_types.total;
                
                // Build model Object
                const modelFields = {};
                modelFields.service_type = service_type;
                modelFields.service_cost = service_cost;
                modelFields.number_of_hrs = number_of_hrs;
                modelFields.total = total;
    
                //Add new model
                service_types = new service_types(modelFields);
               
            }); */